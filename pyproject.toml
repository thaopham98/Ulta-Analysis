[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "ulta-analysis"
version = "0.1.0"
description = "Reproducible Ulta product, variant, and ingredient collection"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "requests>=2.31,<3",
]

[project.scripts]
ulta-scrape = "ulta_analysis.scraping.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
